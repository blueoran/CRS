{

    "type": "object",
    "properties": {
        "response": {
            "type": "string",
            "description": "response to the user's request",
            "additionalProperties": false
        }
    },
        "required": ["response"]
}
