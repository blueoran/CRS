{

    "type": "object",
    "properties": {
        "Product Types": {
            "type": "array",
            "items": {"type": "string", "minItems": 1},
            "description": "Types of the products that might be utilized to effectively achieve the desired goal and fulfill the user's requests.",
            "additionalProperties": false
        }
    },
        "required": ["Product Types"]
}
