{

    "type": "object",
    "properties": {
        "recommend score":{
            "type": "integer",
            "description": "The likelihood that the user requires the recommendation system instead of just a chatbot",
            "additionalProperties": false
        }
    },
        "required": ["recommend score"]
}
