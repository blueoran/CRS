{

    "type": "object",
    "properties": {
        "Prompt": {
            "type": "string",
            "description": "Prompt for the Vector Database to select the most appropriate product that can achieve the goal.",
            "additionalProperties": false
        }
    },
        "required": ["Prompt"]
}
