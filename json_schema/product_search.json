{

    "type": "object",
    "properties": {
        "Prompt": {
            "type": "string",
            "description": "Prompt that describes the features of the potential products for the Vector Database to select the most appropriate product that can achieve the goal.",
            "additionalProperties": false
        }
    },
        "required": ["Prompt"]
}
